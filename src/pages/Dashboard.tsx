import AppLayout from "../layouts/AppLayout";

const Dashboard = () => {
  const contarinerStyles = {
    height: "100vh",
    width: "100%",
  };
  return (
    <div className="" style={contarinerStyles}>
      <AppLayout />
    </div>
  );
};

export default Dashboard;
